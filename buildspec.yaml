version: 0.2

phases:
    install: 
      commands:
        - apt-get install -y mono-complete
        - chmod +x build.sh
    build:
      commands:
        - mono
        - dotnet --list-sdks
        - dotnet --version
        - ls
        - ./build.sh


