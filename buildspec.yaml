version: 0.2

phases:
    install:
      commands:
        - echo Entered the install phase...
        - apt-get update -y
    build:
      commands:
        - cd $CODEBUILD_SRC_DIR
        - ls
        - chmod +x build.sh
        - ./build.sh


